import type { CollectionConfig } from 'payload'

export const MediaContact: CollectionConfig = {
  slug: 'mediaContact',
  admin: {
    group: 'Storage'
  },
  access: {
    read: () => true,
  },
  fields: [
    {
      name: 'alt',
      type: 'text',
      required: true,
    },
  ],
  upload: true,
}
